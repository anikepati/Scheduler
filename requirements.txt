# Core async runtime
asyncpg==0.29.0          # Fastest async Postgres driver
httpx==0.27.0            # Async HTTP client
aiohttp==3.9.5           # Health server

# Scheduling
croniter==2.0.5          # Robust cron expression parsing

# Retry
tenacity==8.3.0          # Declarative retry with backoff

# Validation
pydantic==2.7.1          # Config and domain models

# Logging
structlog==24.2.0        # JSON structured logging

# Dev / testing only
pytest==8.2.0
pytest-asyncio==0.23.7
